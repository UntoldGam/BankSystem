package Frames;
import javax.swing.*;

import Base.UserAccount;

public class DepositFrame extends JFrame {
    DepositFrame(UserAccount user, Float amount) {
        /*
         * Get balance
         * Use a variable to hold the old balance
         * Add the amount to the balance
         * Show new balance
         * Return button
         */
    }
}
